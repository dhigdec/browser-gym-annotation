# Copy to infra/.env (gitignored) and set your key to enable the live agent
# re-run (M6b). Without it, re-run uses the deterministic gold path.
ANTHROPIC_API_KEY=sk-ant-...
AGENT_MODEL=claude-haiku-4-5-20251001
